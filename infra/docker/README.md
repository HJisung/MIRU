# Application container images

Application Dockerfiles will be added with executable services. Use multi-stage, non-root, reproducible builds and keep FFmpeg tooling isolated to media worker images.

